dataDir=/data
dataLogDir=/datalog
# TickTime: ZK 内部时间单位（毫秒）
tickTime=2000
# Session 失效时间 = tickTime * sessionTimeout
# 5个ticket, 客户端必须在initLimit * tickTime 连接上
initLimit=5
# 集群模式用，如果 Follower tickTime*syncLimit 秒 内没响应 Leader 的心跳，就会被 Leader 标记为掉线。
syncLimit=2
autopurge.snapRetainCount=3
autopurge.purgeInterval=0
maxClientCnxns=60
standaloneEnabled=true
admin.enableServer=true
clientPort=2181
# Four Letter Word 白名单：3.5+ 默认全部禁用，需要显式放开下面这些
# 才能让健康检查 (ruok) 和运维诊断 (mntr/srvr/stat/conf) 走 2181 端口的明文协议。
4lw.commands.whitelist=ruok,mntr,srvr,stat,conf
#server.1=localhost:2888:3888
#server.2=ip2:2888:3888
