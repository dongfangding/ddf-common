# 基础脚手架优化执行计划

## 目标

- 收敛基础层职责，避免 `core` 演变成全量基础设施桶。
- 保留“开箱即用”体验，不要求上层业务手工拼装大量依赖。
- 把模块边界、自动配置、测试门禁和文档一致性一次性梳顺。

## 当前主要问题

- 根构建默认关闭测试，导致测试链路长期失真。
- `ddf-common-core` 混入了 `jdbc`、`mysql`、`mail`、`actuator` 等重型能力。
- `mvc` 混入了 `druid` 数据源依赖，职责不纯。
- 多个 starter 依赖 `@ComponentScan`，自动装配粒度过粗。
- 一些模块把强依赖声明成 `optional`，契约不稳定。
- 文档、版本、许可证、编码存在不一致问题。

## 模块重构原则

- `api` 只放协议、DTO、异常、约束、公共枚举。
- `core` 只放轻量公共能力。
  - 包括工具类、公共模型、线程池辅助、缓存抽象、通用 Spring 支撑。
  - 不再直接绑定数据库、邮件、监控、特定数据源。
- 基础设施能力单独拆为独立 starter。
- 上层应用默认只引入“场景 starter”，而不是手动拼多个底层模块。

## 能力拆分归宿

### 1. 数据库基础设施

从 `ddf-common-core` 和 `ddf-common-mvc` 挪到新模块：

- `ddf-common-data-mysql-starter`

统一承载：

- `spring-boot-starter-jdbc`
- `mysql-connector-j`
- `druid-spring-boot-3-starter`
- JDBC 通用扩展
- MySQL 默认配置
- Druid 默认配置

职责：

- 固化当前项目的数据技术栈，不再把 `jdbc/mysql/druid` 分拆成多个小 starter
- 提供统一的数据库接入入口，业务侧不手工拼装依赖
- 通过显式 `AutoConfiguration` 暴露 JDBC 和数据源增强能力

说明：

- `mysql` 和 `druid` 是当前明确固定栈，拆太细没有收益
- `druid` 是数据源实现，不应该待在 `mvc`
- `mysql-connector-j` 不应继续放在 `core`

### 2. 治理层

从 `ddf-common-core` 挪到新模块：

- `ddf-common-governance-starter`

统一承载：

- `spring-boot-starter-mail`
- `spring-boot-starter-actuator`
- 邮件发送能力
- 健康检查扩展
- 应用指标、线程池监控、治理辅助能力
- 后续新增的治理型基础设施

职责：

- 作为统一“治理层”容器，后续 `mail`、监控、审计、告警、应用元信息等都往这里收
- 对业务应用暴露一组稳定治理能力，而不是零散 starter
- 支持只启用其中一部分功能

## 这些能力如何“无感回来”

原则不是让上层手工导入一堆底层模块，而是提供少量聚合 starter。

### 推荐分层

- `ddf-common-core`
  - 轻量核心层
- `ddf-common-starter-web`
  - 聚合 `api + core + mvc + authentication + limit`
- `ddf-common-data-mysql-starter`
  - 聚合数据接入固定栈
- `ddf-common-governance-starter`
  - 聚合 `mail + actuator + 治理扩展`

### 面向业务应用的最终使用方式

典型 Web 服务只需：

```xml
<dependency>
    <groupId>io.github.dongfangding</groupId>
    <artifactId>ddf-common-starter-web</artifactId>
    <version>${revision}</version>
</dependency>
```

如果是常规 MySQL Web 服务：

```xml
<dependency>
    <groupId>io.github.dongfangding</groupId>
    <artifactId>ddf-common-starter-web</artifactId>
    <version>${revision}</version>
</dependency>

<dependency>
    <groupId>io.github.dongfangding</groupId>
    <artifactId>ddf-common-data-mysql-starter</artifactId>
    <version>${revision}</version>
</dependency>
```

如果还需要治理能力：

```xml
<dependency>
    <groupId>io.github.dongfangding</groupId>
    <artifactId>ddf-common-governance-starter</artifactId>
    <version>${revision}</version>
</dependency>
```

如果你希望继续保留“一个依赖覆盖大多数服务”的体验，可以新增：

- `ddf-common-starter-default`

聚合：

- `starter-web`
- `data-mysql-starter`
- `governance-starter`

这样业务应用仍然可以“一把梭”引一个 starter，但基础库内部边界已经被拆干净。

## 治理层的装配约束

治理层必须满足下面这个原则：

- 引入 `ddf-common-governance-starter` 后，即使业务没有配置 `mail` 或部分治理参数，也不能报错

具体方式：

- 邮件 Bean 用 `@ConditionalOnProperty` 控制
  - 例如只有 `spring.mail.host` 或 `customizer.governance.mail.enabled=true` 存在时才创建邮件发送相关 Bean
- 监控扩展 Bean 用 `@ConditionalOnClass` 和 `@ConditionalOnBean` 控制
  - 例如只有 Actuator 存在且相关基础 Bean 可用时才注册扩展能力
- 所有治理配置类都要有安全默认值
  - 默认关闭主动发送、默认不开启外部告警、默认不开启需要环境依赖的扩展
- 对外提供的治理能力分“基础能力”和“增强能力”
  - 基础能力：可无配置启动
  - 增强能力：有配置才生效

换句话说，治理层 starter 的设计不是“引入后全量生效”，而是“引入后能力可用，配置后增强项再开启”。

## 为什么不用继续把这些能力放在 core

- `core` 一旦绑定数据库和治理依赖，就很难被纯工具型模块、轻服务、SDK 型模块复用。
- 任意一个基础设施升级都会波及全部下游。
- 测试矩阵会被放大，启动条件也会越来越重。
- 后续增加多数据源、多数据库方言、多监控实现时，`core` 会持续膨胀。

## 自动配置改造方式

现状大量依赖 `@ComponentScan`。改造目标：

- starter 只暴露显式 `AutoConfiguration`
- 用 `@Bean`、`@Import`、`@EnableConfigurationProperties` 注册组件
- 用 `@ConditionalOnClass`、`@ConditionalOnMissingBean`、`@ConditionalOnProperty` 控制装配
- 避免整包扫描把实现细节无差别暴露给 Spring 容器

## 分阶段实施

### Phase 1：基线修复

- 移除根 POM 全局跳测配置
- 修复基础模块测试依赖和无法编译的测试
- 修复 README 编码、版本、许可证信息
- 建立最小 CI，只校验 `api/core/mvc/authentication/limit`

### Phase 2：核心层瘦身

- 从 `ddf-common-core` 移出 `jdbc`、`mysql`、`mail`、`actuator`
- 从 `ddf-common-mvc` 移出 `druid`
- 新增 `ddf-common-data-mysql-starter`
- 新增 `ddf-common-governance-starter`

### Phase 3：starter 收口

- 引入 `starter-web`、`data-mysql-starter`、`governance-starter`
- 视业务常见组合新增 `starter-default`
- 梳理 README 示例，默认让业务方只接入场景 starter

### Phase 4：自动配置整改

- 替换 `@ComponentScan` 为显式注册
- 清理错误的 `optional` 依赖
- 给每个 starter 补条件装配测试

### Phase 5：仓库治理

- 清理重复 DTO/Topic 模型
- 修正 `comm.ddf` 等命名错误
- 增加 Enforcer、依赖收敛、重复类检测、Jacoco、ArchUnit

## 具体落地决策

### `core` 保留什么

- 通用工具
- 通用异常支撑
- Spring 上下文辅助
- 线程池与优雅停机
- 纯内存缓存辅助
- 不依赖具体中间件的抽象接口

### `core` 不再保留什么

- 数据库驱动
- JDBC 与数据源实现
- 邮件与治理 starter

## 推荐执行顺序

1. 先修测试和文档，不然重构没有反馈闭环。
2. 先拆 `core` 中的 `jdbc/mysql/mail/actuator`，再拆 `mvc` 中的 `druid`。
3. 先产出 `data-mysql-starter` 和 `governance-starter`，再删除旧依赖，避免上层接入体验退化。
4. 最后再做自动配置精修和重复代码治理。

## 验收标准

- 业务应用不需要手动拼装底层依赖，仍然通过 1 到 3 个 starter 完成接入。
- `ddf-common-core` 不再直接依赖 `jdbc/mysql/mail/actuator/druid`。
- 各 starter 的自动装配可通过条件测试验证。
- `ddf-common-governance-starter` 在零配置场景下可安全引入，不因缺少 `mail` 配置报错。
- 根 README、模块 README、POM 元数据保持一致。
- 基础模块测试默认可执行且可通过。
## 已确认的改造决策

### 1. 注入改造边界

- 非抽象类中的字段 `@Autowired`，优先改为构造函数注入。
- 抽象类中的 `@Autowired`，如果去掉后会牵动子类构造器、改变继承层级行为，则暂不改动。
- 单个可选依赖，优先使用 `Optional<T>` 进行构造注入。
- 运行时扩展点、延迟获取、默认工厂兜底等场景，保留 `ObjectProvider<T>`，不机械替换为 `Optional<T>`。

### 2. 自动配置治理边界

- starter 入口默认不再使用 `@ComponentScan`，优先使用 `@Bean`、`@Import`、`@EnableConfigurationProperties`。
- 扫描的是本仓库自有实现类时，按显式注册方式收口。
- 扫描的是第三方包时，不做机械替换；只有在明确接管第三方 Bean 装配时，才进一步改造。

### 3. 当前仓库改造状态

- 除 `ddf-common-captcha` 中扫描第三方包 `com.anji.captcha` 的场景外，其余 starter 入口的 `@ComponentScan` 已完成收口。
- 已完成收口的模块包括：`core`、`s3`、`third-party`、`api`、`mvc`、`authentication`、`ons`、`websocket`、`alarm`、`zookeeper`、`mongo`、`mqtt-client`、`rocketmq`、`netty-broker`、`vps`、`mqtt`。

### 4. 后续继续改造时的默认策略

- 先做结构收口，再做语义优化，优先保证行为不变。
- 遇到可选依赖时，先判断是否属于“单个可选依赖”还是“运行时扩展点”。
- 遇到抽象类继承链注入时，默认不主动下钻修改子类构造器。
